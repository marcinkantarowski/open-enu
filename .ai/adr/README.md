# This project's decisions

One file per decision, `{NNNN}-{kebab-title}.md`, numbered from 0001 independently of the
platform - so a platform update that adds its own next number never collides with yours.
Read [the platform's records](../platform/adr/README.md) first; to depart from one, write a
record here that names it rather than editing it.

| # | Decision |
|---|---|
