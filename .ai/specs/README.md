# This project's specs

One file per non-trivial feature, `{YYYY-MM-DD}-{kebab-title}.md`, written **before** the
work. `make module NAME=X` writes the stub here. Format and purpose are the same as
[the platform's](../platform/specs/README.md), which describe the modules that shipped with it.

| Spec | Date |
|---|---|
