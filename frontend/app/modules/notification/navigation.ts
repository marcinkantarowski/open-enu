import { defineNavigation } from '@ui-kit/composables/defineNavigation'

// No menu entry: the bell in the header is the way in, and a second route to
// the same list in the sidebar is noise.
export default defineNavigation([])
