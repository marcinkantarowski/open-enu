import { defineNavigation } from '@ui-kit/composables/defineNavigation'

// No top-level entry: this module's screen lives under Settings, where people
// look for it, rather than competing with the features they came to use.
export default defineNavigation([])
