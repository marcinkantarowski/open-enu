import { defineNavigation } from '@ui-kit/composables/defineNavigation'

// No top-level entry: webhooks are configuration, and configuration lives under
// Settings where people look for it.
export default defineNavigation([])
