{
  "//": "Nuxt writes the real compiler options into .nuxt on `nuxt prepare`; this file only points at them. `nuxt typecheck` looks for it here and fails outright if it is absent.",
  "files": [],
  "references": [
    { "path": "./.nuxt/tsconfig.app.json" },
    { "path": "./.nuxt/tsconfig.server.json" },
    { "path": "./.nuxt/tsconfig.shared.json" },
    { "path": "./.nuxt/tsconfig.node.json" }
  ]
}
